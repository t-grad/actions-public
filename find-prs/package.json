{
  "name": "find-pr",
  "version": "0.1.0",
  "private": true,
  "description": "Action to parse",
  "main": "lib/main.js",
  "scripts": {
    "clean": "rm -rf lib/ dist/",
    "build": "tsc",
    "package": "ncc build --source-map --license licenses.txt",
    "test": "jest --passWithNoTests"
  },
  "dependencies": {
    "@actions/core": "^1.4.0",
    "@actions/github": "^4.0.0"
  }
}
